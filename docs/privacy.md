# Privacy

DockCat processes only events submitted directly through its own UI, internal API, or custom URL. It does not read Notification Center databases, inspect other applications, capture the screen, use OCR, inject code, or use private APIs. Notification content remains on the Mac and is not transmitted. Logs contain identifiers, source category, and state changes—not full private message bodies.
